#include <memory.h>
#include <iostream>
int main() {
    int n;
    scanf("%d", &n);
    int *a = new int[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    int *b = new int[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &b[i]);
    }
    int *c = new int[n];
    for (int i = 0; i < n; i++) {
    }
}